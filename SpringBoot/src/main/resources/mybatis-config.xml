<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <settings>
        <!-- 这个配置使全局的映射器启用或禁用缓存 -->
        <setting name="cacheEnabled" value="true" />
        <!-- 对于未知的SQL查询，允许返回不同的结果集以达到通用的效果 -->
        <setting name="multipleResultSetsEnabled" value="true"/>
        <!-- 配置默认的执行器。SIMPLE 执行器没有什么特别之处。REUSE 执行器重用预处理语句。BATCH 执行器重用语句和批量更新 -->
        <setting name="defaultExecutorType" value="REUSE" />
        <!-- 全局启用或禁用延迟加载。当禁用时，所有关联对象都会即时加载。 -->
        <setting name="lazyLoadingEnabled" value="false" />
        <setting name="aggressiveLazyLoading" value="true" />
        <!-- <setting name="enhancementEnabled" value="true"/> -->
        <!-- 设置超时时间，它决定驱动等待一个数据库响应的时间。 -->
        <setting name="defaultStatementTimeout" value="25000" />
        <setting name="logImpl" value="LOG4J2" />
    </settings>
    
    <!-- typeAliases 
    <typeAliases>
    	<typeAlias alias="BookHis" type="com.entity.book.BookHis"/>
    	<typeAlias alias="Book" type="com.entity.book.Book"/>
    	<typeAlias alias="BookTypeHis" type="com.entity.book.BookTypeHis"/>
    	<typeAlias alias="BookType" type="com.entity.book.BookType"/>
    	
    	<typeAlias alias="UserBookDetailRelation" type="com.entity.ub.UserBookDetailRelation"/>
    	<typeAlias alias="UserBookRelation" type="com.entity.ub.UserBookRelation"/>
    	
    	<typeAlias alias="User" type="com.entity.user.User"/>
    </typeAliases> 
    -->
    <!-- typeHandlers
    <typeHandlers>
    	<package name="com.handler"/>
    	<typeHandler handler="" />
    </typeHandlers>
    -->
    <!-- Mybatis 分页插件  -->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageHelper">
            <property name="dialect" value="mysql"/>
            <property name="pageSizeZero" value="true"/>
            <property name="reasonable" value="true"/>
        </plugin>
    </plugins>
    
    <!-- mappers  -->
    <!-- 
    <mappers>
    	<mapper resource="com/mapper/book/BookHisMapper.xml"/>
    	<mapper resource="com/mapper/book/BookMapper.xml"/>
    	<mapper resource="com/mapper/book/BookTypeHisMapper.xml"/>
    	<mapper resource="com/mapper/book/BookTypeMapper.xml"/>
    	
    	<mapper resource="com/mapper/ub/UserBookDetailRelationMapper.xml"/>
    	<mapper resource="com/mapper/ub/UserBookRelationMapper.xml"/>
    
    	<mapper resource="com/mapper/user/UserMapper.xml"/>
    </mappers> 
    -->
   
    <!-- 
    properties?,settings?,typeAliases?,typeHandlers?,
    objectFactory?, objectWrapperFactory?,plugins?,
    environments?,databaseIdProvider?,mappers 
    -->
    
</configuration>